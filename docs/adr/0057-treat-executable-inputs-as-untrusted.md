# Treat executable inputs as untrusted

An approved Plan authorizes an operation but does not establish that its artifacts, Actions, provider responses, remote hosts, or network inputs are safe. Provision verifies immutable identity, isolates execution where possible, delegates short-lived least-privilege credentials, and keeps secrets out of durable state. Weaker isolation is an explicit policy fallback rather than an invisible property of a target.
