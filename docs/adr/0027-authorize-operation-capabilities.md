# Authorize operation capabilities rather than fixed roles

Provision defines capabilities for viewing, planning, deploying, approving, destroying, adopting, refreshing data, rotating secrets, and managing policy while identity and group membership remain external. Environments decide which capabilities may be combined and which operations require approval. This supports both small teams and separation-of-duties policies without encoding organization-specific job titles as product roles.
