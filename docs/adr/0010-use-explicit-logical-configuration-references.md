# Use explicit logical configuration references

Configuration is composed from explicitly referenced logical documents. Teams may store those documents in one physical file or many files, but Provision does not infer meaning from filenames or automatically scan directories. This makes the resolved environment inspectable and avoids hidden configuration appearing because a file was renamed, moved, or added.
