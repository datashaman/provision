# Drive changes with explicit push operations

Provision changes environments through observe, plan, approve, and execute push operations rather than a universal pull controller. Interrupted work resumes from its journal and fresh provider observations. Opt-in reconciliation may exist later, but it still creates visible recorded operations. This preserves explicit user intent and drift review without requiring an always-running management plane.
