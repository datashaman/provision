# Implement the engine in Go

The local engine, CLI, and future runner use Go. Its supported desktop and Linux targets, official AWS SDK, typed compilation, and suitability for distributable operational binaries align with the local-first architecture. This chooses deployment simplicity and one shared implementation over a scripting runtime or separate client and server language stacks.
