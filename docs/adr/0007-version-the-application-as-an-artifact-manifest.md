# Version the application as an artifact manifest

An application revision is an immutable manifest identifying the complete set of component artifacts for one application version. Individual artifacts may be reused when a component is unchanged, but promotion and environment history refer to the complete revision so partial changes cannot create an unrecorded mixture of application versions.

